import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Header = () => {
  return (
    <View style={styles.headerContainer}>
      <Text style={styles.name}>Vaishnavi Bajpai</Text>
      <Text style={styles.contact}>Email: bajpai.98@example.com</Text>
      <Text style={styles.contact}>Phone: 123 456 7890</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  headerContainer: {
    alignItems: "center",
    marginBottom: 20,
  },
  name: {
    fontSize: 24,
    fontWeight: "bold",
  },
  contact: {
    fontSize: 16,
  },
});

export default Header;
